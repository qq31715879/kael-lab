# Kael Lab Agent Entry

Kael Lab 是公开静态站点。处理本仓库时遵守以下边界：

- 当前实现必须保持可由 GitHub Pages 直接发布，不依赖服务端、私密凭据或运行时构建。
- 任何内容进入仓库前都必须已经是公开派生物；不得复制内部绝对路径、组织上下文、凭据、私有截图或未确认原文。
- Meepo 与其他项目只作为事实来源；本仓库不得反向修改其源码或把概念展示写成当前已实现能力。
- `Current`、`Concept`、`Roadmap` 的语义必须保持清楚；无法证明的内容不得标记为 `Current`。
- 修改页面后至少检查桌面与移动布局、键盘操作、控制台错误、静态资源路径和无 JavaScript 的基础可读性。
- 发布、域名、第三方统计、评论、外部 API、提交、推送和删除仍服从当前任务的明确授权。
- `AGENTS.md` 与 `CLAUDE.md` 内容必须保持一致。
